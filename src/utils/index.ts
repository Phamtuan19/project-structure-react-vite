/**
 * Các hàm trong utils không phụ thuộc vào bất kỳ logic cụ thể nào của ứng dụng.
 *
 * Ví dụ: formatDate, debounce, clampNumber, slugify
 * các hàm này hoạt động độc lập và có thể dùng trong mọi dự án,
 * mọi phần của hệ thống.
 *
 */

export * from './cn';

export * from './cookies-storage';
