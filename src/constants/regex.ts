/**
 * @deprecated todo remove later
 *
 * Export những gì các feature khác được phép dùng
 *
 */

/**
 *
 * Regex username: ít nhất 3 ký tự, gồm chữ, số, dấu _ hoặc -
 *
 */
export const USER_NAME_REGEX = /^[a-zA-Z0-9._-]{3,}$/;

/**
 *
 * Regex email: chuẩn cơ bản
 *
 */
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
