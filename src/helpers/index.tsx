/**
 * helpers có thể phụ thuộc vào context hoặc domain cụ thể của ứng dụng,
 * ví dụ như xử lý form, user, authentication,...
 *
 * Ví dụ: buildUserPayload, validateLoginForm, getModalTitleFromStatus
 *
 */
